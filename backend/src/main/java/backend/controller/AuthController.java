package backend.controller;

// AuthController is intentionally left empty.
// The /users/login/body endpoint was consolidated into UserController
// to avoid Spring MVC conflicts from two beans sharing @RequestMapping("/users").
public class AuthController {
}
